# 3Lines — 마이크로 저널 앱 PRD

> **문서 버전**: v3.0
> **최종 수정**: 2026-03-28
> **상태**: Active — v1.0 구현 완료 기준

---

## 1. 프로젝트 개요

### 1.1 앱 컨셉

**3Lines**는 하루 3줄로 마음의 회복탄력성(Mental Resilience)을 키우는 마이크로 저널 앱이다.

매일 **감사(Gratitude)**, **수용(Acceptance)**, **의도(Intention)** 세 가지 축으로 한 줄씩 기록하고, 오늘의 감정을 체크하면 끝. 30초 안에 완료할 수 있어 부담이 없고, 시간이 쌓이면 자신의 감정 패턴과 내면의 성장을 발견할 수 있다.

### 1.2 핵심 가치

| 가치 | 설명 |
|------|------|
| **30초 완료** | 길게 쓸 필요 없다. 한 줄이면 충분하다. |
| **마음 회복탄력성** | 감사·수용·의도의 3축 기록으로 심리적 근력을 키운다. |
| **시각적 성취감** | GitHub 잔디처럼 기록이 쌓이는 걸 눈으로 확인한다. |
| **자기 발견** | 감정 추이, 자주 쓰는 단어, 요일별 패턴 등 인사이트를 제공한다. |
| **완전한 프라이버시** | 모든 데이터는 로컬 저장. 서버 없음. 계정 없음. |

### 1.3 타겟 유저

**페르소나: "지친 직장인 수진" (28세)**
- 저널링에 관심 있지만 매일 길게 쓰는 건 부담스러움
- 자기 관리/성장에 관심 있지만 시간이 없음
- 습관 형성 앱을 좋아하고, 시각적 보상에 동기부여를 받음
- 가끔 감정 기복이 있어서 자기 패턴을 알고 싶음

**확장 타겟:**
- 자기 관리/성장에 관심 있는 20~30대
- 마음챙김/명상에 관심을 갖기 시작한 사람
- 심리 상담과 병행하며 일상 기록을 남기고 싶은 사람

### 1.4 경쟁 분석 (업데이트)

| 앱 | 장점 | 약점 | 3Lines 차별점 |
|----|------|------|--------------|
| **Five Minute Journal** | 구조화된 AM/PM 3줄 프롬프트, 습관화 설계 우수 | 유료, 영어 중심, 서버 의존 | 한국어 네이티브, 무료, 100% 로컬 |
| **Daylio** | 감정+활동 태깅, 2탭 UI, 30초 완료 | 텍스트 기록 약함, 구독형, 구체적 성찰 부족 | 3줄 텍스트 + 감정의 조합, 심층 성찰 유도 |
| **Stoic** | 스토아 철학 기반 프롬프트, 아침/저녁 루틴 | 철학적 장벽, 한국어 미지원 | 심리학 기반 한국어 3축 프레임워크 |
| **Reflectly** | AI 개인화 프롬프트, 감정 트래킹 | 구독 필수, 서버 의존, 프라이버시 우려 | 프라이버시 우선, 오프라인 완결 |
| **Journey** | AI 인사이트, 풍부한 미디어 첨부 | 너무 많은 기능, 유료, 서버 의존 | 극도의 단순함, 30초 핵심 루프 |
| **Grid Diary** | 그리드 레이아웃 프롬프트, 시각적 구조 | 자유도 낮음, 유료, 한국어 약함 | 유연한 커스터마이징 + 한국어 최적화 |
| **Day One** | 풍부한 기능, 사진/위치, 암호화 | 너무 많은 기능, 유료, 클라우드 의존 | 극도의 단순함, 무료, 완전 로컬 |
| **Pixels** | 감정 히트맵 시각화 | 텍스트 기록 없음, 성찰 부재 | 히트맵 + 의미 있는 3축 텍스트 |

### 1.5 성공 지표 (KPIs)

| 지표 | 목표 (출시 3개월 내) | 측정 방법 |
|------|---------------------|-----------|
| 7일 연속 기록률 | 40% 이상 | 로컬 통계 (인사이트 화면) |
| 30일 연속 기록률 | 15% 이상 | 로컬 통계 |
| 평균 작성 시간 | 30초 이내 | createdAt - 화면 진입 시간 |
| 앱 스토어 평점 | 4.5+ | App Store / Google Play |
| 일일 복귀율 (DAU/MAU) | 50% 이상 | 로컬 통계 |

> **참고**: 프라이버시 우선 정책상 외부 분석 SDK 없음. 모든 측정은 로컬 데이터 기반.

---

## 2. 타겟 플랫폼 & 제약사항

### 2.1 플랫폼

| 플랫폼 | 최소 지원 버전 | 비고 |
|--------|---------------|------|
| iOS | 16.0+ | iPhone만 지원 (iPad 로드맵 P3 참조) |
| Android | API 26 (8.0)+ | 태블릿 미지원 (v1.0) |

### 2.2 지원 언어

- **v1.0**: 한국어만
- **향후**: 영어, 일본어 (PRD 8장 로드맵 P3 참조)

### 2.3 오프라인 정책

- **100% 오프라인**: 네트워크 연결 불필요 ✅ 구현완료
- 서버 통신 없음, 계정 시스템 없음
- 데이터 백업은 JSON 내보내기/가져오기로 해결 ✅ 구현완료

---

## 3. 유저 스토리 & 핵심 플로우

### 3.1 유저 스토리

| ID | 유저 스토리 | 우선순위 | 상태 |
|----|-----------|---------|-------|
| US-01 | 사용자로서, 앱을 열면 바로 오늘의 3줄을 작성할 수 있다 | Must | ✅ 구현완료 |
| US-02 | 사용자로서, 오늘의 감정을 5단계로 선택할 수 있다 | Must | ✅ 구현완료 |
| US-03 | 사용자로서, 이미 작성한 기록을 수정할 수 있다 | Must | ✅ 구현완료 |
| US-04 | 사용자로서, 기록 히스토리를 히트맵으로 한눈에 볼 수 있다 | Must | ✅ 구현완료 |
| US-05 | 사용자로서, 과거 특정 날짜의 기록을 다시 볼 수 있다 | Must | ✅ 구현완료 |
| US-06 | 사용자로서, 감정 추이와 통계 인사이트를 볼 수 있다 | Must | ✅ 구현완료 |
| US-07 | 사용자로서, 매일 리마인더 알림을 받을 수 있다 | Should | ✅ 구현완료 |
| US-08 | 사용자로서, 질문을 커스터마이징할 수 있다 | Should | ✅ 구현완료 |
| US-09 | 사용자로서, 데이터를 JSON으로 내보내고 가져올 수 있다 | Should | ✅ 구현완료 |
| US-10 | 사용자로서, 다크모드를 사용할 수 있다 | Should | ✅ 구현완료 |
| US-11 | 사용자로서, 모든 데이터를 삭제할 수 있다 | Must | ✅ 구현완료 |
| US-12 | 사용자로서, 첫 실행 시 앱 소개를 볼 수 있다 | Should | ✅ 구현완료 |
| US-13 | 사용자로서, 생체인증으로 앱을 잠글 수 있다 | Should | ✅ 구현완료 |
| US-14 | 사용자로서, 과거 기록을 키워드로 검색할 수 있다 | Should | ✅ 구현완료 |

### 3.2 핵심 유저 플로우

**플로우 1: 첫 실행**
```
앱 설치 → 스플래시 → 온보딩 3페이지 (fade+slide 전환) → "시작하기" → Today Screen
```

**플로우 2: 일일 기록 (핵심 루프)**
```
앱 열기 → Today Screen
├── 미작성 상태
│   → 감정 선택 (1~5)
│   → 질문 1 답변 입력 (탭 가능 제안 문구로 빈 페이지 마찰 감소)
│   → 질문 2 답변 입력
│   → 질문 3 답변 입력
│   → "기록 완료" 탭
│   → 저장 완료 애니메이션
│   → 읽기 모드 전환
└── 작성 완료 상태
    → 기록 확인 (읽기 모드)
    → "수정하기" 탭 → 편집 모드 → 저장
```

**플로우 3: 과거 기록 조회**
```
Timeline 탭 → 히트맵 확인 → 특정 셀 탭 → BottomSheet로 상세 보기
또는: 검색 아이콘 → 키워드 입력 → 결과 리스트 → 상세 보기
```

**플로우 4: 인사이트 확인**
```
Insights 탭
├── 7일 미만 → 잠금 상태 + 프로그레스 표시
└── 7일 이상 → 감정 추이 차트 + 통계 카드 + 요일별 차트 + 키워드 분석
```

---

## 4. 화면별 상세 명세

### 4.1 온보딩 (첫 실행 시만) ✅ 구현완료

**활성화 조건**: `onboarding_done` 설정이 `"false"`일 때

| 페이지 | 일러스트/아이콘 | 타이틀 | 서브타이틀 |
|--------|---------------|--------|-----------|
| 1 | 연필 아이콘 | "하루 3줄로 나를 기록하세요" | "감사, 수용, 의도 — 30초면 충분해요" |
| 2 | 차트 아이콘 | "감정의 흐름을 한눈에" | "히트맵과 인사이트로 나를 발견해요" |
| 3 | 방패 아이콘 | "당신만의 공간" | "모든 데이터는 기기에만 저장돼요" |

- PageView + 하단 dot indicator ✅
- 마지막 페이지에 "시작하기" ElevatedButton ✅
- 스킵 기능: 우측 상단 "건너뛰기" TextButton ✅
- 화면 간 fade+slide 전환 애니메이션 ✅

### 4.2 Today Screen (메인 화면) ✅ 구현완료

**목적**: 오늘의 저널을 빠르게 작성한다.

**화면 구성** (위→아래 순서):

1. **상단 인사말 영역** ✅
   - 시간대별 인사:
     - 05:00~11:59 → "좋은 아침이에요"
     - 12:00~17:59 → "좋은 오후예요"
     - 18:00~04:59 → "좋은 저녁이에요"
   - 날짜 표시: "2026년 3월 28일 토요일" (intl 포맷)
   - 연속 기록일 배지: "12일 연속" (1일 이상일 때만 표시) ✅

2. **감정 피커** (`emotion_picker.dart`) ✅
   - 가로 배치 5개 원형 버튼
   - 감정 매핑 (muted 컬러 팔레트):

     | 값 | 이모지 | 라벨 | 심리 상태 | 색상 |
     |----|--------|------|----------|------|
     | 1 | 😫 | 힘듦 | Anxious/Stressed | `#C4736A` (dusty rose) |
     | 2 | 😕 | 불안 | Uneasy | `#C49A6A` (warm sand) |
     | 3 | 😐 | 보통 | Neutral | `#B5A97E` (muted gold) |
     | 4 | 🙂 | 평온 | Calm | `#7EA88A` (sage) |
     | 5 | 😊 | 감사 | Grateful/Joyful | `#5B8A6A` (deep sage) |

   - 미선택: 회색 톤, 선택 시 해당 색상 활성화
   - 선택 애니메이션: scale 1.0→1.2→1.0 (200ms, easeOut) ✅

3. **질문 카드 3개** (`prompt_card.dart`, 스크롤 가능) ✅
   - **기본 질문 (Mental Resilience 3축)**:
     - **감사**: "오늘 감사한 작은 것 하나는?"
     - **수용**: "오늘 불편했던 감정을, 있는 그대로 인정한다면?"
     - **의도**: "내일 내가 되고 싶은 모습은?"
   - 각 카드 구성:
     - 질문 카테고리 태그 (감사/수용/의도, 작은 Chip)
     - 질문 텍스트 (bodyLarge, weight 500, opacity 0.8)
     - TextField (maxLines: 2, maxLength: 200)
     - 글자 수 카운터 (opacity 0.5)
   - 카드 스타일: surface color, borderRadius 12dp, 간격 12dp
   - 탭 가능 제안 문구 (로드맵 P0 — 빈 페이지 마찰 감소)

4. **저장 버튼** ✅
   - 하단 고정 ElevatedButton (full-width, 패딩 16dp)
   - 텍스트: "기록 완료"
   - **활성화 조건**: 감정 선택 + 최소 1개 답변 입력
   - 비활성 시 시각적 피드백 (회색 + disabled 상태)
   - 저장 성공: 체크마크 draw 애니메이션 (`completion_animation.dart`, 600ms, elasticOut) → 읽기 모드 전환 ✅

5. **이미 작성한 경우** (읽기 모드) ✅
   - 상단 "오늘의 기록 완료" 배지 (surfaceVariant 배경)
   - 저장된 감정 + 답변이 읽기 전용으로 표시
   - "수정하기" OutlinedButton → 편집 모드 전환

**상태 흐름**:
```
앱 진입 → DB에서 오늘 엔트리 조회
├── 엔트리 없음 → 빈 입력 폼 표시
│   → 감정 선택 → 답변 입력 → 저장 → 완료 애니메이션 → 읽기 모드
└── 엔트리 있음 → 읽기 모드
    └── "수정하기" → 편집 모드 → 저장 → 읽기 모드
```

**에지 케이스**:
- 자정을 넘기면 자동으로 새로운 날의 빈 폼으로 전환 (앱이 포그라운드일 때)
- 답변 입력 중 앱이 백그라운드로 가면 → 입력 중인 내용은 컨트롤러 상태에 유지 (DB 저장은 명시적 "기록 완료" 시에만)
- 키보드가 올라올 때 레이아웃 처리 → SingleChildScrollView + resizeToAvoidBottomInset
- 매우 긴 텍스트(200자) 입력 시 카드 높이 → maxLines: 2로 제한, 오버플로우 없음

---

### 4.3 Timeline Screen (타임라인) ✅ 구현완료

**목적**: 기록 히스토리를 한눈에 보고, 과거 기록을 되돌아본다.

**화면 구성**:

1. **상단 스트릭 영역** ✅
   - 현재 연속 기록일: 큰 숫자 (displaySmall, bold)
   - "현재 연속" 라벨
   - 옆에 최장 기록일: "최장 45일" (bodySmall)
   - `streak_badge.dart` 컴포넌트

2. **기간 토글** (SegmentedButton) ✅
   - "12주" / "6개월" / "1년"
   - 기본값: 12주

3. **히트맵 그리드** (`heatmap_grid.dart`) ✅
   - GitHub 잔디 스타일 레이아웃
   - 행(세로): 요일 (월~일, 7행)
   - 열(가로): 주차
   - 왼쪽 요일 레이블: 월, 수, 금만 표시
   - 각 셀: 정사각형
   - 색상 매핑: 감정 점수 → warm sage gradient (5.2 컬러 시스템 참조)
   - 하단 색상 범례: "적음 □□□□□ 많음"
   - **셀 크기 동적 계산**: (화면 너비 - 좌우 패딩 32dp - 요일 레이블 30dp) ÷ 주차 수

4. **셀 탭 인터랙션** ✅
   - 기록 있는 날짜 탭 → `entry_detail_sheet.dart` BottomSheet
     - 날짜 + 감정 이모지
     - 질문/답변 3쌍 표시
   - 기록 없는 날짜 탭 → 무반응

5. **검색 기능** ✅
   - 검색 아이콘 탭 → 검색 입력 모드 전환
   - 키워드로 과거 기록 전문 검색
   - 결과 리스트 → 항목 탭 → BottomSheet 상세 보기

**에지 케이스**:
- 데이터 0건일 때 → 빈 히트맵 + "첫 기록을 시작해보세요" 안내
- 1년 모드에서 셀이 매우 작아질 때 → 탭 영역 최소 44x44dp 보장 (접근성)
- 히트맵 데이터 로딩 중 → Skeleton shimmer 효과

---

### 4.4 Insights Screen (인사이트) ✅ 구현완료

**목적**: 기록 데이터를 분석하여 자기 발견 인사이트를 제공한다.

**활성화 조건**: 최소 7일 이상 기록

#### 빈 상태 / 잠금 상태 (7일 미만) ✅ (`insights_locked_view.dart`)
- 아이콘 (Icons.insights, 크게)
- "7일 이상 기록하면 인사이트가 열려요"
- 프로그레스: "3/7일 완료" + LinearProgressIndicator

#### 활성 상태 (스크롤 가능)

1. **감정 추이 차트** (fl_chart LineChart) ✅ (`emotion_trend_chart.dart`)
   - 기간 선택: "1주" / "1개월" / "3개월" (ChoiceChip)
   - X축: 날짜, Y축: 감정 점수 (1~5)
   - 부드러운 곡선 (isCurved: true)
   - 터치 시 해당 날짜 값 툴팁
   - 기록 없는 날은 빈 구간 (선 끊김)

2. **통계 카드 그리드** (2×2 GridView) ✅ (`stat_card.dart`)

   | 카드 | 내용 | 아이콘/장식 |
   |------|------|-----------|
   | 평균 감정 | 최근 7일 평균 (소수점 1자리) | 해당 감정 이모지 |
   | 현재 스트릭 | 연속 기록일 | 불꽃 아이콘 |
   | 총 기록 | 전체 엔트리 수 | 노트 아이콘 |
   | 최고의 요일 | 평균 감정 최고 요일 | 달력 아이콘 |

3. **요일별 감정 바 차트** (fl_chart BarChart) ✅
   - X축: 월~일
   - Y축: 평균 감정 점수
   - 바 색상: 점수에 따라 muted emotionColors 매핑

4. **키워드 분석** ("자주 쓰는 단어") ✅ (`keyword_cloud.dart`)
   - Wrap + Chip 위젯
   - 빈도 상위 10개
   - 칩 크기/색상 강도로 빈도 표현
   - 한국어 불용어 제거 (조사, 접속사, 일반 부사 등)
   - 어근 추출, 조사 분리 처리 (`text_analysis.dart`)
   - 접근성: semanticLabel 포함

5. **감사 키워드 TOP 5** ✅ (`gratitude_keywords_list.dart`)
   - 첫 번째 질문(감사) 답변에서만 추출
   - 리스트: 순위 + 단어 + 등장 횟수

**에지 케이스**:
- 정확히 7일째 기록 시 → 인사이트 활성화 + 축하 메시지
- 모든 감정이 동일값일 때 → 차트가 평탄선, 정상 표시
- 특정 요일 데이터 없을 때 → 해당 바 높이 0으로 표시
- 답변이 모두 빈 문자열일 때 → 키워드 섹션 "아직 분석할 키워드가 부족해요" 표시

---

### 4.5 Settings Screen (설정) ✅ 구현완료

**목적**: 앱 개인화와 데이터 관리.

**화면 구성** (Material 3 ListTile 기반, 섹션별 구분선):

1. **섹션: 질문 설정** ✅
   - 질문 1~3 각각 ListTile (subtitle에 현재 질문 텍스트 표시)
   - 탭 → TextFormField 다이얼로그 (maxLength: 100)
   - "기본값으로 초기화" TextButton (확인 다이얼로그 포함)

2. **섹션: 알림** ✅
   - 매일 리마인더 Switch (flutter_local_notifications)
   - 리마인더 시간 (TimePicker, Switch ON일 때만 활성)
   - 알림 텍스트: "오늘의 3줄을 기록할 시간이에요"
   - 기본값: OFF, 21:00

3. **섹션: 외관** ✅
   - 테마: SegmentedButton ("시스템 기본" / "라이트" / "다크")

4. **섹션: 보안** ✅
   - 생체인증 잠금 Switch (local_auth — Face ID / 지문)
   - 지원 여부 자동 감지, 미지원 기기에서는 비활성

5. **섹션: 데이터** ✅
   - "데이터 내보내기 (JSON)" → JSON 파일 생성 → share_plus로 공유 시트
   - "데이터 가져오기 (JSON)" → 파일 피커 → 파싱 → 가져오기 확인 다이얼로그
   - "모든 데이터 삭제" → 2단계 확인:
     1. "정말 삭제하시겠어요?" AlertDialog
     2. "삭제"를 직접 입력해야 확인 가능

6. **섹션: 정보** ✅
   - 앱 버전 (package_info_plus)
   - "오픈소스 라이선스" → LicensePage

**JSON Export/Import 포맷**:
```json
{
  "app": "3Lines",
  "version": "1.0.0",
  "exported_at": "2026-03-28T21:00:00+09:00",
  "total_entries": 45,
  "entries": [
    {
      "date": "2026-03-28",
      "emotion": 4,
      "prompts": [
        {"category": "gratitude", "question": "오늘 감사한 작은 것 하나는?", "answer": "맑은 날씨"},
        {"category": "acceptance", "question": "오늘 불편했던 감정을, 있는 그대로 인정한다면?", "answer": "회의 때 긴장했다"},
        {"category": "intention", "question": "내일 내가 되고 싶은 모습은?", "answer": "침착한 사람"}
      ],
      "created_at": "2026-03-28T21:30:00+09:00"
    }
  ]
}
```

---

## 5. UI/UX 디자인 가이드

### 5.1 디자인 원칙

1. **명상적 미니멀리즘**: 여백을 충분히 활용, 한 화면에 하나의 행동에 집중
2. **시각적 보상**: 기록 완료 시 만족스러운 애니메이션, 히트맵의 성취감
3. **부드러운 전환**: 모든 상태 변화에 자연스러운 애니메이션
4. **비처벌적 언어**: 기록 공백에도 부드러운 격려 메시지, 심리적 안전감 유지

### 5.2 컬러 시스템

**테마 seedColor**: `#5B6E5D` (muted sage green)
**폰트**: Google Fonts Noto Sans (한국어 최적화)

**감정 색상** (감정 피커 + 차트 — muted/sophisticated 팔레트):
| 감정 값 | 색상 이름 | 헥스 코드 |
|---------|----------|----------|
| 1 (힘듦) | Dusty Rose | `#C4736A` |
| 2 (불안) | Warm Sand | `#C49A6A` |
| 3 (보통) | Muted Gold | `#B5A97E` |
| 4 (평온) | Sage | `#7EA88A` |
| 5 (감사) | Deep Sage | `#5B8A6A` |

**히트맵 색상** (라이트 모드 — warm sage gradient):
| 단계 | 색상 | 헥스 코드 |
|------|------|----------|
| 없음 | Warm Grey | `#E8E4DC` |
| 1단계 | | `#D6E5D8` |
| 2단계 | | `#B0CEB5` |
| 3단계 | | `#8BB892` |
| 4단계 | | `#68A170` |
| 5단계 | | `#4D8F5C` |

**히트맵 색상** (다크 모드 — warm sage gradient):
| 단계 | 색상 | 헥스 코드 |
|------|------|----------|
| 없음 | Dark Warm | `#2A2826` |
| 1단계 | | `#1F3425` |
| 2단계 | | `#2A4A33` |
| 3단계 | | `#366042` |
| 4단계 | | `#437752` |
| 5단계 | | `#508C5C` |

### 5.3 타이포그래피

Material 3 기본 타이포그래피 사용 (Noto Sans):

| 용도 | TextStyle | 크기 | Weight |
|------|-----------|------|--------|
| 인사말 | headlineSmall | 24sp | 400 |
| 날짜 | titleMedium | 16sp | 500 |
| 질문 텍스트 | bodyLarge | 16sp | 500 |
| 답변 입력 | bodyLarge | 16sp | 400 |
| 통계 큰 숫자 | displaySmall | 36sp | 700 |
| 카드 라벨 | labelMedium | 12sp | 500 |
| 캡션 | bodySmall | 12sp | 400 |

### 5.4 애니메이션 명세

| 상황 | 애니메이션 | Duration | Curve |
|------|-----------|----------|-------|
| 감정 선택 | scale 1.0→1.2→1.0 | 200ms | easeOut |
| 저장 완료 | 체크마크 draw (completion_animation) | 600ms | elasticOut |
| 화면 전환 | fade + slide up | 300ms | easeInOut |
| 온보딩 전환 | fade + slide (커스텀 PageRoute) | 300ms | easeInOut |
| 히트맵 셀 탭 | 셀 하이라이트 + sheet up | 250ms | easeOut |
| 카드 등장 | staggered fade in | 100ms interval | easeOut |
| 스트릭 카운터 | 숫자 카운트업 | 800ms | decelerate |
| 마일스톤 축하 | 가벼운 confetti / 이모지 버스트 (로드맵 P1) | 1000ms | easeOut |

### 5.5 네비게이션

하단 NavigationBar (Material 3), 4개 탭:

| 탭 | 아이콘 | 라벨 | 경로 |
|----|--------|------|------|
| Today | Icons.edit_note | 오늘 | `/` |
| Timeline | Icons.grid_view | 타임라인 | `/timeline` |
| Insights | Icons.insights | 인사이트 | `/insights` |
| Settings | Icons.settings | 설정 | `/settings` |

---

## 6. 앱 라이프사이클 & 에지 케이스

### 6.1 자정 전환

- 앱이 포그라운드 상태에서 자정(00:00)을 넘기면:
  - Today Screen: 새로운 날의 빈 폼으로 자동 전환
  - 입력 중이던 미저장 데이터는 유실 (의도적 — 어제의 기록은 어제 저장해야 함)
  - 구현: `WidgetsBindingObserver`의 `didChangeAppLifecycleState` + 타이머

### 6.2 백그라운드/포그라운드

- 백그라운드 진입 시: 특별한 처리 없음 (상태는 메모리에 유지)
- 포그라운드 복귀 시: 날짜 변경 여부 체크 → 변경 시 상태 리프레시
- 생체인증 잠금 활성화 시: 포그라운드 복귀 시 인증 재요청

### 6.3 알림

- flutter_local_notifications 사용 ✅ 구현완료
- 매일 반복 알림 (사용자 설정 시간)
- 이미 오늘 기록 완료한 경우에도 알림 발송 (로컬에서 조건 체크 불가)
- 알림 탭 → 앱 열기 → Today Screen
- **스트릭 위험 알림** (로드맵 P1): 설정 시간 1시간 전, 미기록 시 추가 알림

### 6.4 데이터 보호

- 앱 삭제 시 모든 데이터 소멸 (iOS/Android 기본 동작)
- 스크린샷 방지: 미적용 (v1.0)
- **앱 잠금 (생체인증)**: ✅ 구현완료 — Face ID/지문 인증 (`biometric_service.dart`, local_auth 패키지)

---

## 7. 접근성 요구사항

- 모든 인터랙티브 요소에 `Semantics` / `semanticLabel` 적용 ✅
- 감정 피커: "감정 선택: 5단계 중 4 (평온)" 형태의 접근성 레이블 ✅
- 히트맵 셀: "3월 28일, 감정 4점" 형태 ✅
- 키워드 클라우드: 각 칩에 빈도 포함한 semanticLabel ✅
- 최소 터치 영역: 44×44dp (Material Guidelines) ✅
- 색상만으로 정보 전달하지 않기 (히트맵에 수치 툴팁 보완) ✅
- 다이나믹 폰트 사이즈 지원 (MediaQuery.textScaleFactor 대응)

---

## 8. 고도화 로드맵 v2 (레퍼런스 기반)

### P0 — 즉시 구현 ✅ 모두 완료

| 기능 | 설명 | 참고 레퍼런스 | 상태 |
|------|------|--------------|------|
| **스트릭 유예일 (Grace Day)** | 1일 미기록 시 스트릭 유지 — 완벽주의 부담 해소 | Duolingo, Streaks 앱 | ✅ 구현완료 |
| **비처벌적 스트릭 언어** | 공백 후 복귀 시 "자리를 비웠어도 괜찮아요, 다시 돌아오신 것만으로 충분해요" 메시지 | Finch, Daylio | ✅ 구현완료 |
| **프롬프트 탭 가능 제안 문구** | 프롬프트 카드 하단에 탭 가능한 예시 문구 제공 → 빈 페이지 마찰 감소 | Five Minute Journal | ✅ 구현완료 |
| **주간 감정 델타 표시** | Insights에 "지난주 대비 이번주 평균 감정 +0.3" 배지 표시 | Reflectly | ✅ 구현완료 |

### P1 — 1차 업데이트 ✅ 모두 완료

| 기능 | 설명 | 참고 레퍼런스 | 상태 |
|------|------|--------------|------|
| **"1년 전 오늘" 회고** | 1년 전 같은 날 기록이 있으면 Today Screen 상단 카드 표시 | Day One, Journey | ✅ 구현완료 |
| **마일스톤 축하** | 7일, 30일, 100일, 365일 달성 시 그라데이션 배너 + 축하 메시지 | Duolingo, Streaks | ✅ 구현완료 |
| **스트릭 위험 알림** | 리마인더 1시간 전, 미기록+스트릭 보유 시 사전 알림 (별도 채널) | Habitica | ✅ 구현완료 |
| **월간 감정 요약 카드** | Insights 하단에 이번 달 평균 감정, 기록 수, TOP 키워드 요약 | Daylio, Reflectly | ✅ 구현완료 |

### P2 — 2차 업데이트 (일부 완료)

| 기능 | 설명 | 상태 |
|------|------|------|
| **주간 회고 (로컬)** | 템플릿 기반 주간 요약 — 감정 추이, TOP 키워드, 최고/최저 날 (100% 오프라인) | ✅ 구현완료 |
| **사진 일기 모드** | 하루 1장 사진 첨부 (카메라/갤러리), DB 마이그레이션 v2, 타임라인 상세에 표시 | ✅ 구현완료 |
| **홈 화면 위젯** | iOS/Android 위젯으로 오늘의 질문 표시, 빠른 감정 체크 | ✅ 구현완료 |
| **월간 리포트 PDF** | 한 달 기록을 PDF로 생성 및 공유, 감정 분포 차트 + 일별 기록 포함 | ✅ 구현완료 |

### P3 — 향후

| 기능 | 설명 |
|------|------|
| **iCloud/Google Drive 백업** | 클라우드 동기화 옵션 (기본은 여전히 로컬) |
| **다국어** | 영어, 일본어 질문 세트 + UI 번역 |
| **iPad/태블릿 지원** | 적응형 레이아웃 (두 패널 구조) |
